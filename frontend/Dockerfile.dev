# Dockerfile de développement pour le frontend React avec Vite
FROM node:18-alpine

WORKDIR /app

# Copier les fichiers de dépendances et les installer
COPY package*.json ./
RUN npm install

# Copier le script de génération depuis le répertoire racine
COPY ../scripts/generate-build-info.sh ./scripts/

# Copier le reste du code source
COPY . .

# Régénérer automatiquement les informations de build
RUN chmod +x scripts/generate-build-info.sh && ./scripts/generate-build-info.sh

# Exposer le port du serveur de développement Vite
EXPOSE 5173

# Démarrer le serveur de développement
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
